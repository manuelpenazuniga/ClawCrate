# ClawCrate
Terrified of AI agents executing malicious code? 🦀 ClawCrate is a secure Rust sandbox that isolates LLM-generated code using native Linux kernel primitives (Landlock + seccomp-bpf). Protect your local system from vulnerabilities. Zero Docker, zero root, single static binary. Secure any AI agent (like OpenClaw) in under 2 minutes
