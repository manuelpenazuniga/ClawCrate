# Notes

Benign project notes. Safe for the MCP filesystem server to read.

The point of the demo is that the server can read this file, but cannot read the
secrets in `.env`, `.npmrc`, or the sibling `secret-vault/` directory.
