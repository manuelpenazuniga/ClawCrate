; FIXTURE ONLY — not a real token. Planted to demonstrate .clawcrateignore
; exclusion from the Replica workspace on both Linux and macOS.
//registry.npmjs.org/:_authToken=fixture-only-not-a-real-npm-token
