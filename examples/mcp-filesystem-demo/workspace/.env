# FIXTURE ONLY — not a real secret. Planted to demonstrate that ClawCrate keeps
# it out of the Replica workspace the MCP server sees.
API_TOKEN=fixture-only-not-a-real-secret
DB_PASSWORD=demo-placeholder-do-not-use
